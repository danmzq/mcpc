{
  "schemaVersion": 1,
  "id": "mcpc_storage",
  "version": "${version}",
  "name": "MCPC Storage Automation",
  "description": "Embodied storage automation dengan Fabric + Baritone bridge",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.mcpc.storage.StorageAutomationMod"
    ]
  },
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}",
    "fabric-api": "*"
  }
}
